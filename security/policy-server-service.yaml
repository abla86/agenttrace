apiVersion: v1
kind: Service
metadata:
  name: agenttrace-policy-server
  namespace: agenttrace
spec:
  selector:
    app: agenttrace-policy-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
