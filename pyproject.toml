[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="agenttrace"
version="0.1.0"
description="Deterministic adaptive security evaluation laboratory for AI agents"
requires-python=">=3.10"
[project.scripts]
agenttrace="agenttrace.cli:main"
