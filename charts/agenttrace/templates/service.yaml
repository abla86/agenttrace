apiVersion: v1
kind: Service
metadata:
  name: {{ include "agenttrace.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agenttrace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agenttrace.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
